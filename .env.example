# Leave this unset for ordinary local builds; the account UI then reports that
# cloud login is not configured. Production cloud builds must use the exact
# root HTTPS API origin and also export AARRE_CLOUD_RELEASE=1 in the shell/CI.
VITE_AARRE_API_BASE_URL=https://sync.nexvoice.cc
