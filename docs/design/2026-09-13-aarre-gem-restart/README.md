# Aarre · 完整宝石的新方向

本轮直接使用内置 `image_gen.imagegen` 接口生成。原始 PNG 未被代码重绘或修改。

保留两版作为造型候选：

- [02 · 斜切琥珀](02.png)：不规则外轮廓、中央主切面和边缘切面，使用同一琥珀色系。
- [04 · 红色原石](04.png)：稍柔和的原石轮廓，使用同一红色系的少量切面。

蓝色 01 过于接近方块，紫色 03 更像标签，均不作为推荐方向；原稿仍保留。

[生成提示词](prompts.json) · [原始输出记录](outputs.json)

本轮仍为候选，插件图标和构建没有替换。选定后需进一步精修轮廓、透明底和小尺寸适配。
