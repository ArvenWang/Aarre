# Aarre 字标候选与品牌配色

打开 [对比页](index.html)，切换四套字标与日间/夜间主题。

## 字标：已采用 A（0.6.25）

| 方案 | 字体 | 预览设置 | 菜单字号 |
| --- | --- | --- | --- |
| A | Fraunces Italic | weight 600，SOFT 100，WONK 1，opsz 24 | 20px |
| B | Lora Italic | weight 600 | 21px |
| C | Alegreya Italic | weight 600 | 23px |
| D | Cormorant Garamond Italic | weight 600 | 24px |

用户已选 A。正式品牌名使用独立 AarreWordmark 组件：菜单 20px、完整收藏库 22px，正文与会话标题保持。固定 Fraunces 的四个轴后仅保留 Aarre 所需字形，WOFF 2,188 字节，随扩展内嵌加载；元数据见 `selected-font.json`，OFL 许可随 `public/fonts/OFL-Aarre-Wordmark.txt` 分发。A 菜单与设置截图已更新为 0.6.25 实际扩展；B/C/D 保留早期候选截图。

## 配色：0.6.24 白色开关圆钮

| 用途 | 日间 | 夜间 |
| --- | --- | --- |
| 主题强调文字 `--accent` | #B74624 | #DEDEDE |
| 活跃控件填色 `--accent-solid` | #F2633D，与 logo 一致 | #DEDEDE |
| 开关圆钮 `--scrim-ink` | #FFFFFF | #FFFFFF |
| 开启轨道 `--toggle-active-track` | #F2633D | 中灰，品牌灰与深灰混合 |
| 成功状态 `--positive` | 跟随 `--accent` | 跟随 `--accent` |

深橙文字对白底对比度约 5.36:1；原始品牌橙对白底约 3.17:1，用于非文字控件填色。主操作按钮与焦点环保持中性。现有 AI 标签、已收藏状态、推荐文件夹、同步状态等继承共享颜色；首个图表系列跟随品牌填色，其余分类系列和错误语义色保留。所有开关的开启/关闭圆钮均为白色；暗色开启轨道使用中灰，保证与白钮的对比度。单独安装 Aarre 的浮动宿主同步主题；与 NexAlign 合并时沿用共享中性把手，未修改共享模块或 NexAlign。

## 验证

- 设计 token 检查、类型检查、正式构建与构建产物 JS 语法检查通过。
- 0.6.24 实际独立 Chrome：15 项目标检查通过，包含明暗 420/320px 正式字标加载/对齐、正文隔离、完整收藏库字标、两种开关状态的白色圆钮；未记录到页面异常。白钮与开启轨道对比度为日间 3.17:1、夜间 3.60:1。
- 对比页：四个字体载入、8 种字体/主题切换、截图载入均通过；390px/320px 没有横向溢出，已目视检查。
- 0.6.24：实际扩展明暗 420/320px 字标、菜单/网页版图标对齐、开关开启/关闭的白色圆钮与轨道对比度均做目标检查。会话标题的字体隔离已检查渲染分支与样式作用域，未调用 AI 创建会话。A 与配色图均来自正式构建，未注入字体样式。
- 未重载日常 Chrome、修改用户收藏或调用在线 AI；未提交、推送、发布。重新加载扩展并刷新网页后，新配色生效。

浏览器脚本与报告、修改前备份和构建记录位于 `work/brand-type-theme-20260914/`。

0.6.24 本轮备份、构建与实际浏览器记录位于 `work/brand-wordmark-20260914/`。

0.6.25：按用户要求把菜单字标从 22px 缩至 20px、完整版从 26px 缩至 22px；字体、图标和白色圆钮保持。正式构建与明暗 420/320px、网页版目标检查通过。
