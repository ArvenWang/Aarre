# Aarre 0.6.5 · 方形快捷栏、收藏入口与顶部重排

本轮只处理 2026-09-11 最新三张图片的批注，保留已完成的右侧网页内菜单、单一对话工作区、分组设置与长回答换行。当前截图见 [验收图集](index.html)，19 张均使用图像查看工具逐张检查，不包含用户个人浏览截图。

## 需求与可观察目标

| 目标 | 实现 | 验证与结果 |
| --- | --- | --- |
| N1 等大正方形、磨砂 | 快捷栏 52 × 100px；两个按钮各 44 × 44px、图标 20 × 20px。收起时半透明加 16px 背景模糊；展开后的正文使用实色。 | 明暗主题按钮尺寸一致，图标中心偏差 0。条纹底图可见透色与模糊。减少透明效果时实色、无滤镜。[量测](logs/glass-buttons.json) |
| N2 顶部重新排列 | 品牌/双导航在左，外观/备份/设置在右；搜索与文件夹/排序成一组，处理状态和低强调的封面工具在下一行。小屏自动换行。 | 1440/768/320px × 明暗 6 组均无页面横溢出。主导航文字居中，选中为细下划线。实际搜索 Figma 返回 1 条并更新状态数量；主题图谱与设置入口正常。[量测](logs/manager-layout.json) |
| N3 星标先打开表单 | 星标展开同一个菜单，进入“添加到收藏”页面。用户确认名称、文件夹和备注后才调用保存；已有记录进入“管理此收藏”。 | 首次打开、暖启动、取消、重复请求保留草稿、保存失败/重试通过；初始收藏视图通知不会吞掉打开请求。实际 Chrome 核验见下文。 |
| N4 内容与稳定性 | 请求保留到应用与来源标签就绪；读取最新备注后显示已有收藏；错误显示在表单内，底部操作固定可见。 | 400px 菜单与 320px 宿主窗口（可用宽 305px）明暗四组无横溢出，调整尺寸后备注相同。39 项关联专项通过；既有来源校验/启动超时保留。[量测](logs/save-layout.json) |
| N5 视觉、安装态、封装分别核验 | 复用 HeroUI 和现有正式收藏控制器；截图、真实 Chrome、构建分别记录。 | 19 张实际渲染截图逐张检查；安装版 0.6.5 已核验；正式封装结果由统一进展文档与 outputs 清单回填。 |

表单滚动实测：鼠标滚动后内容偏移 0→113px，正文宽度保持 400px，操作栏位置不变；自制滚动条 5px、底轨透明、闲置渐隐。见 [滚动核验](logs/save-scrollbars.json)。图集 1150/420px 下均 19/19 图片加载成功，无横溢出。

## 实际 Chrome

- 原扩展 ID `ppjmhonejgpcdmjmcbbdjookgiagambm`，加载目录 `/Users/nefish/Desktop/Coding/Aarre/dist`，通过原生扩展管理界面从 0.6.4 重载为 **0.6.5**。没有附加浏览器调试器，避免干扰扩展 iframe 的来源验证。
- 独立公开页面 `https://example.com/?aarre-qa=20260911-065`：点击星标，首次界面读取约 1.6 秒内已经显示真实名称/文件夹/备注表单。取消、收起，再次点击星标进入表单，未提前添加书签。
- 填写并核对测试名称 `Aarre QA 0.6.5 收藏表单验证` 与测试备注，点击“添加到 Chrome”。随后星标显示“管理此收藏”，自定义名称、文件夹和备注完整保留。刷新网页后再次点击星标也完整恢复，显示“不会创建重复收藏”。
- 通过 Chrome 原生书签窗口核对相同名称后，只移除此测试书签；工具栏恢复“为此标签页添加书签”。没有删除其他书签。测试网页及本轮创建的扩展管理/收藏库标签已关闭，返回原来的 FOLD 页面。
- 正式扩展的完整收藏库也已打开并目视检查新版顶部。用户原有菜单宽度保留，未修改主题、账号、模型或权限配置。
- 原生工具曾出现剪贴板粘贴时序问题：名称在保存前核对发现不一致，改用直接设置输入值后再次确认，才执行保存；这属于验收工具问题。未将工具的操作尝试当成产品成功证据。
- 此次安装验证的 95 个构建文件 SHA-256 保存在 [安装态构建记录](logs/installed-build-files.json)，用于与最终封装逐个对照。

## 检查方法与边界

- 图集由正式 React/HeroUI UI 与正式宿主代码渲染，收藏数据、接口为明确标注的 DEV 替身；[宿主验收页面](host-harness.html) 仅可在开发服务运行。透明条纹用于观察磨砂，不属于产品页面内容。故障截图由明确的失败传输返回，不声称真实服务故障。
- 类型检查、设计 token 检查、构建与产物语法已通过；后台为 300,901 bytes，低于 330,000 硬门。专项覆盖 23 项几何/协议、8 项宿主恢复/意图、8 项真实 React 工作区流程。
- 最终封装从干净本地提交执行 `npm run package:artifacts`，包含完整回归。产物版本、ZIP/解压目录、大小/哈希与源码提交独立校验，以 `AGENT_PROGRESS.md` 当前节和 `outputs/Bookmark-Layer-0.6.5-*` 为最终记录。
- 没有新增真实 AI 聊天、跨客户端云同步/恢复、其他系统或商店发布验收。书签保存沿用产品现有后台增强流程，不能据此宣称模型效果、云同步或所有功能都已验收。
- 本轮只做本地提交与本机更新，未推送、合并或发布；0.6.4 和更早的产物保持不变。

## 实现参考

背景磨砂遵循 [MDN backdrop-filter](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/backdrop-filter) 对半透明背景的要求；配合 [prefers-reduced-transparency](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/%40media/prefers-reduced-transparency) 和强制颜色回退。媒体查询支持取决于浏览器/系统；此次在实际浏览器模拟减少透明效果并核对实色回退。
