# Keep this in /etc/aarre/backup.env with root:root and mode 600.
# Never expose these variables to the long-running aarre-api service.
TENCENT_BACKUP_SECRET_ID=replace-with-backup-worker-cam-secret-id
TENCENT_BACKUP_SECRET_KEY=replace-with-backup-worker-cam-secret-key
