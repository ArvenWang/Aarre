# Aarre 项目进展

最后更新：2026-07-29

## 当前进展

Chrome 原生书签增强产品已完成 0.2.3 可靠性加固。侧边栏读取 Chrome 的全部原生书签根目录；右键菜单会携带页面或链接上下文自动打开侧边栏编辑表单，用户确认后再创建真实书签。DeepSeek / OpenAI / Gemini BYOK 已改为本地直连，Supabase 和 Google 登录降为可选的跨设备同步能力。真实 DeepSeek 请求仍需用户在扩展设置中填写自己的 Key 后手动验证。

正式产品名：`Aarre`。代码目录与内部包名暂时保留 `Bookmark-Layer`，避免无关路径迁移影响当前扩展。

当前统一项目目录：`/Users/nefish/Desktop/WorkSpace/Coding/Bookmark-Layer`。

## 最近更新

### 2026-07-29 · 编辑信息层级、GitHub 封面与毛玻璃吸顶

- 书签编辑页移除“完整简介”“已完成”和“初始由 AI 生成”等冗余标签；AI 分析说明改为明确区分：主题是 AI 归纳的内容方向，标签是可由用户自行增删的检索关键词。
- 删除书签进入确认态后，原“取消 / 保存”按钮隐藏；确认按钮简化为“取消 / 确认”，移除确认区外层红色底和描边。确认态与普通操作栏都固定为 36px，高度切换不再推动弹窗。
- Chrome `_favicon` 请求增加 `forceEmptyDefaultFavicon=1`，缺少真实 favicon 时不再返回灰色地球；全部候选失败后显示 Aarre 自己的书签品牌图标。
- 站点 favicon 改为 16 DIP、2× 位图读取，并以 16px 居中显示；在 Retina 屏上使用 32 像素源图向下采样，不再把低分辨率图标放大到 32px 造成模糊。
- 列表代表图由 56 × 56 收敛为 48 × 48，列表行继续保持 68px；代表图上下各留 10px，标题和 URL 与整行垂直中心对齐。
- GitHub 仓库优先采用仓库自己的 Open Graph 社交预览图；页面元数据不可读时使用 GitHub 仓库级 OpenGraph 地址兜底，并排除设置、搜索、登录等非仓库路径。
- 全目录扫描会主动重新处理“已有缩略图但不是 GitHub 仓库预览图”的旧记录，已有错误 GitHub 封面无需删除书签即可更新；已有完整 AI 信息时只刷新封面，不重复产生 AI 调用。
- 展开文件夹吸顶层使用独立半透明伪元素与 14px 背景模糊；有文件夹时，列表底部渐隐从会阻断背景采样的 `mask-image` 改为 52px 覆盖层，因此吸顶项可以真正模糊后方内容，抵达底部后覆盖层仍会渐隐消失。
- 本地浏览器验证编辑文案、简化删除确认、真实吸顶毛玻璃、底部渐隐、favicon 尺寸、封面尺寸与垂直居中；删除前后操作栏均为 36px、弹窗无高度跳变。
- `npm run check` 在 Node.js 22.22.2 下通过：13 个测试文件、52 个测试、类型检查和生产构建全部通过。
- 最新生产构建已写入 `dist/`：侧边栏入口为 `assets/sidepanel-BvlMJJDP.js`，样式入口为 `assets/styles-mqX6XqLO.css`。

### 2026-07-29 · 代表图补全与展开文件夹吸顶

- 列表存在文件夹时取消顶部渐隐并移除顶部 3px 内边距，避免吸顶文件夹标题被遮白或从上方露出上一条内容；底部渐隐、自定义滚动条和纯书签平铺列表的顶部渐隐保持不变。
- 代表图提取从仅支持 `og:image / twitter:image`，扩展为社交元数据、`image_src`、JSON-LD 图片和正文主图四级候选；会过滤 logo、icon、头像和追踪像素，并优先选择大尺寸 hero / cover / thumbnail。
- 代表图下载后在扩展后台中心裁切为 112 × 112 WebP，写入本机 IndexedDB；本地缓存不上传云端，云端元数据合并也不会把它覆盖，列表不再依赖站点防盗链和二次网络请求。
- 全目录扫描现在会把“已有 AI 信息但缺代表图”的书签重新纳入队列；补图无需重复调用 AI，只有简介或标签缺失时才调用当前服务商。
- 展开的文件夹标题行使用受自身文件夹范围约束的吸顶定位；向下滚动时停在列表顶部，后续文件夹到达后会自然把前一个标题推走。
- 使用用户参考图中的六个真实网站验证代表图提取，六个均取得有效图片 URL；其中 `good-web-design.com` 没有社交卡片，已通过正文缩略图回退成功提取。
- `npm run check` 在 Node.js 22.22.2 下通过：13 个测试文件、49 个测试、类型检查和生产构建全部通过；最新构建已写入 `dist/`。
- 本地 433 × 909 预览实测：滚动 650px 后展开文件夹标题仍固定在内容顶部；同时展开下一文件夹并继续滚动后，前一标题被推到顶部外、下一标题接替吸顶。

### 2026-07-29 · Chrome 代表图式书签列表

- 书签列表按 Chrome 新版侧栏的代表图结构重做：每行固定 68px，左侧 56 × 56 网页代表图，右侧只显示单行标题与域名 URL；扫描前后不再改变高度。
- 列表移除卡片描边和常驻底色，默认保持白底，Hover 使用 `#F6F7FA`、Active 使用 `#EEF0F4`。
- 编辑按钮继续在 Hover / 键盘聚焦时渐显，保留半透明毛玻璃底，但完全移除投影。
- 新增 `SiteThumbnail`：优先显示资源中的真实页面代表图，加载失败或缺失时使用 Chrome 本地 favicon 降级。
- 对照 Chromium 原生实现确认 Chrome 使用内部 `PageImageService` 和 Optimization Guide 取得页面代表图，并非整页截图；扩展无法调用该内部 Mojo 接口。
- Aarre 全目录扫描新增 `og:image` / `twitter:image` 与页面 favicon 提取，扫描完成后会写入本地资源并自动显示在列表缩略图中，不依赖第三方截图服务。
- `npm run check` 通过：13 个测试文件、44 个测试、类型检查和生产构建全部通过；本地 433 × 909 预览控制台无错误。
- 视觉对照记录见 `design-qa.md`，结果为 `passed`；生产构建已写入 `dist/`，侧边栏入口为 `assets/sidepanel-CwXVNc9W.js`，样式入口为 `assets/styles-CNzEM6a_.css`。

### 2026-07-29 · 书签卡片信息精简与悬浮编辑

- 书签列表不再渲染 URL、AI 简介、扫描等待或失败文案；未扫描卡片只显示站点图标和标题，扫描完成后额外显示全部标签。
- 卡片取消描边，默认使用 `#F6F7FA` 底色，Hover 使用 `#EEF0F4`；继续保持无投影。
- 扫描前卡片高度收敛为 62px，扫描后含标签卡片为 84px 起，并继续根据标签换行自然增高。
- 编辑按钮改为卡片右上角绝对定位的悬浮控件；桌面端默认完全隐藏，卡片 Hover 或键盘聚焦时显示。
- 编辑按钮使用 64% 白色半透明底、12px 背景模糊和轻微饱和度增强；Hover 后提高不透明度。无 Hover 设备仍保留可操作入口。
- 完整 `npm run check` 通过：13 个测试文件、44 个测试、类型检查和生产构建全部通过。
- 本地浏览器实测卡片默认 `rgb(246,247,250) / 0px border`，Hover 为 `rgb(238,240,244)`；编辑按钮由 `opacity: 0` 过渡到 `1`，URL 与简介节点均不存在，控制台无错误。
- 生产构建已重新写入 `dist/`，侧边栏入口更新为 `assets/sidepanel-Di9tLbg6.js`，样式入口为 `assets/styles-C6g_Uv9S.css`。

### 2026-07-29 · 侧栏标题、Hover 与三家 AI 配置统一

- 侧栏列表主标题由 `Aarre` 改为“我的书签”；对话页改为“收藏对话”。历史会话页和设置页移除标题下方的重复小字。
- 收藏、编辑书签和新建文件夹弹窗移除英文眉题；“Chrome 文件夹”统一简化为“文件夹”，下拉框无障碍名称同步更新。
- 设置页移除底部“本地收藏 / 待同步”统计模块，减少与核心配置无关的信息噪声。
- Gemini 取消内置/云端服务分支，与 OpenAI、DeepSeek 一致，必须填写并在线验证 API Key 后才能启用；无 Key 时不会把正文交给云端 Edge Function 兜底处理。
- 卡片 Hover 提高到约 14% 中性色描边和更明确的浅底色；输入框 Hover 使用约 15% 中性描边，聚焦为约 20% 中性描边且无外圈投影。
- 文件夹列表项之间增加 2px 间隔，并加强文件夹 Hover 底色，避免相邻状态紧贴。
- 完整 `npm run check` 在项目要求的 Node.js 22.22.2 下通过：13 个测试文件、44 个测试、类型检查和生产构建全部通过。
- 本地浏览器逐项验证“我的书签”、历史/设置标题、三家 API Key 状态、移除统计模块、收藏/新建文件夹弹窗以及 Hover 样式；控制台无错误。
- 生产构建已重新写入 `dist/`，侧边栏入口更新为 `assets/sidepanel-C6I6mnzD.js`，样式入口为 `assets/styles-Cm5xYXQl.css`。

### 2026-07-29 · 知识卡信息密度与扫描状态收敛

- 书签卡移除全部投影，描边降低到约 5.5% 中性色透明度；悬停只做极轻背景和边界变化，不再产生浮起效果。
- 叶子书签卡取消随文件夹层级继续缩进，仅保留统一的 4px 外侧呼吸空间；文件夹行仍保留树形层级，卡片获得更完整的内容宽度。
- 卡片新增明确的 `pending / ready` 分态：扫描前使用紧凑卡，扫描完成后提高最小高度并根据简介、URL 和标签内容自然增高。
- 标题、AI 简介和 URL 取消单行省略与固定行数裁切，允许自然换行；列表会展示全部标签，不再只截取前三个。
- 本地浏览器实测：扫描完成卡片高度 118px 起，未扫描卡片当前为 89px；二者左边界一致，投影为 `none`，标题与 URL 均为正常换行，控制台无错误。
- 生产构建已重新写入 `dist/`，侧边栏入口更新为 `assets/sidepanel-eoszUdLZ.js`，样式入口为 `assets/styles-B_fv9k7C.css`。

### 2026-07-29 · 书签知识卡、完整 AI 分析与可编辑标签

- 书签叶子节点统一改为精致知识卡：白底、细边框、轻阴影和 16px 圆角，并重新整理站点图标、标题、简介、URL、标签与编辑入口的视觉层级；文件夹仍保持轻量树形结构，避免层级混乱。
- 点击书签编辑入口后会展示完整 AI 简介、主题、处理状态和全部标签；AI 简介与主题为只读信息，标签可以新增、删除和保存。
- 新增标签来源字段 `tagsSource`：首次扫描由 AI 生成；用户调整后标记为用户版本，后续全目录扫描只更新 AI 简介和主题，不覆盖用户整理过的标签。
- 标签修改已接入真实后台与本地 IndexedDB，并在已配置云端时进入现有同步队列；云端表结构和 Edge Function 源码新增 `tags_source` 支持，但迁移尚未部署到真实 Supabase。
- 开发预览补齐标签写入和书签更新响应，已实际验证“展开文件夹 → 打开完整 AI 分析 → 新增标签 → 保存 → 重新打开后显示用户版本”完整交互。
- 生产构建已重新写入 `dist/`，侧边栏入口更新为 `assets/sidepanel-y01aLSNs.js`，样式入口为 `assets/styles-ESi3HJJV.css`。

### 2026-07-29 · 扩展版本错配崩溃修复与纯白底

- 定位真实 Chrome 扩展报错 `Uncaught TypeError: c is not iterable`：新版侧边栏请求旧版后台尚不认识的资源/会话消息时，旧后台以成功状态返回 `undefined`，界面随后把它当数组遍历而崩溃。
- 后续 Chrome 错误页截图仍指向历史文件 `assets/sidepanel-BnCwvv3L.js`；该文件已不在当前项目、最新 `dist` 或历史交付目录中。Chrome 会保留旧扩展错误记录，需重新加载当前 `dist` 并清除历史错误后，再判断是否有新错误复现。
- 扩展消息层新增“成功但无数据”的协议错配拦截，改为明确提示用户在 `chrome://extensions` 重新加载 Aarre，不再进入损坏的渲染状态。
- IndexedDB 资源读取新增旧数据标准化：早期仅有名称和 URL 的记录会自动补齐简介、标签、主题、原生书签关联和状态字段，不要求用户清空书签或数据库。
- 资源列表、扫描更新、书签事件刷新和历史会话加载增加运行时数组校验与错误收敛；侧边栏增加最终恢复页，未知渲染异常也不会再留下空白页。
- 侧边栏页面、根容器、书签主面板和异常恢复页底色统一为纯白；文件夹悬停/选中等必要层级色仍保留。
- 生产构建已重新写入 `dist/`，侧边栏入口更新为 `assets/sidepanel-DC0I3NgR.js`。
- 本地页面验证主面板与根容器均为 `rgb(255, 255, 255)`，Aarre、书签元数据与 Agent 输入框正常渲染，控制台无错误。

### 2026-07-29 · 全目录 AI 扫描与独立会话页

- 对照 `ai-coding-Research` 的收藏智能分析实现，新增可持久恢复的全目录扫描任务：显式启动、实时进度、暂停、继续、取消和错误统计。
- 扫描启动时通过 Chrome `optional_host_permissions` 单独申请网页读取权限；抓取不携带 Cookie，最多读取 600KB HTML，并跳过内网、局域网、带凭据和常见企业内部域名。
- 每个网页先提取描述、站点、H1/H2、首段、关键词与 URL 路径词，再由当前配置的 DeepSeek / OpenAI / Gemini BYOK 生成简介、标签与主题；结果逐条写回本地 IndexedDB。
- 侧边栏书签条目现在完整展示名称、AI 简介、标签和 URL；未扫描、处理中和失败状态不再冒充已有 AI 信息。
- Agent 从“最多 24 条关键词候选”升级为全目录紧凑索引；当前 269 条预览数据全部进入模型上下文，回答回显实际查看数量，并支持最近 10 轮多轮对话。
- 底部发送后不再显示覆盖列表的弹层，改为进入独立会话页；左上角返回列表，回答来源可打开真实收藏。
- 列表右上角新增历史会话入口；会话真实保存在 `chrome.storage.local`，最多 50 个会话、每个 60 条消息。
- 本地预览已验证：元数据四项展示、发送进入会话页、`DeepSeek · 已查看 269/269 条收藏`、返回列表、打开历史会话，以及设置页全目录扫描模块。
- 生产构建已写入 `dist/`；新增页面精华、会话存储与 30 条全目录上下文测试。

### 2026-07-29 · Aarre 品牌名与真实收藏 Agent

- 侧边栏主标题、扩展清单、页面标题、管理页品牌和用户提示统一改为正式产品名 `Aarre`。
- 定位到原底部输入框只会打开管理页做关键词搜索，从未调用已配置的 DeepSeek；这也是 Key 验证成功但 AI 入口仍不可用的根因。
- 新增 `ASK_BOOKMARK_AGENT` 真实链路：后台从本地 IndexedDB 选择相关和最近收藏，把有限的摘要、标签、备注和正文摘录直接交给当前配置的 DeepSeek / OpenAI / Gemini 回答。
- 回答在侧边栏内展示，不再打开新标签页；同时显示实际 AI 服务商和最多 5 条相关收藏来源。
- 管理页原“AI 语义搜索”更名为“云端向量搜索”，明确它属于可选 Supabase 能力；本地 DeepSeek Agent 不再受 Google 登录状态限制。

### 2026-07-29 · DeepSeek 本地直连与可选云端

- 新增扩展后台的 AI 直连链路：用户选择 DeepSeek 并保存 Key 后，收藏页面会直接调用 DeepSeek Chat Completions，并把摘要、标签和主题写回本地 IndexedDB；模型处理发生在 DeepSeek 服务端。
- 同一实现兼容 OpenAI 与 Gemini BYOK；网页正文按 50,000 字符上限传给所选服务商，并明确隔离网页内 Prompt Injection。
- 未配置 Supabase 时，原生书签和 AI 信息仍完整保存，不再创建永远无法完成的云端 Outbox，也不再显示虚假的待同步数量。
- 以后配置并登录 Supabase 时，本地资源会自动转为待同步并补传；云端继续作为跨设备备份与 pgvector 语义搜索的可选增强。
- AI Key 无效、额度不足、超时或结构化响应异常时，Chrome 书签仍会成功保存，并在侧边栏显示非破坏性提示。
- 设置页明确标注 Google 账号和 Supabase 为可选项，BYOK 调用由扩展直接发往所选 AI 服务商。
- 修复生产构建配置 Supabase 时覆盖三家 AI 官方域名权限的问题，改为保留并合并精确 `host_permissions`。
- 新增 DeepSeek 本地直连、Key 拒绝和未配置 Key 的单元测试。

### 2026-07-29 · 多 AI 服务商与焦点样式

- AI 设置从单一 Gemini 扩展为 Gemini、OpenAI、DeepSeek 三个直接可见的预设；每家提供默认模型并允许手动修改。
- OpenAI 默认使用适合摘要与结构化提取的 `gpt-5.6-luna`，DeepSeek 默认使用 `deepseek-v4-flash`；模型名按 2026-07-29 官方文档校准。
- 每家服务的 API Key 独立保存在 `chrome.storage.local`，切换服务不会覆盖其他 Key；旧版 `geminiApiKey` 会自动迁移且完整 Key 仍不会回传给 UI。
- Key 保存前会请求服务商官方模型接口验证权限；DeepSeek 还会检查当前账号是否能访问所选模型。三家服务均必须配置自己的 API Key。
- 扩展到 Edge Function 的 AI 请求增加服务商、模型和临时 Key 协议；云端源码已支持 OpenAI / DeepSeek 的 JSON 摘要与标签生成，语义向量继续统一使用 Gemini 768 维索引，避免混用向量空间。
- 收藏浮层和设置页输入框聚焦统一为单层 1px 中性边框，移除额外 `box-shadow` 外圈，与底部 AI 对话框一致。
- Gemini 已取消“内置服务 / 云端未连接”分支，与 OpenAI、DeepSeek 使用完全一致的 BYOK 状态和保存规则。
- 本地预览已验证三家切换、默认模型、Key 占位提示、未配置时禁用保存，以及输入框 `border-width: 1px / box-shadow: none`。

### 2026-07-29 · 侧边栏滚动、下拉与设置页收口

- 侧边栏主标题移除 `CHROME BOOKMARKS` 和 `Chrome 同步 / 数据源` 标签，只保留“书签栏”。
- 书签列表改为上下双向 52px 渐隐：位于顶部时不遮挡首行，向下滚动后顶部渐隐出现；抵达底部后底部渐隐自动撤销。
- 隐藏系统原生滚动条，新增距右侧 8px 的 5px 自定义手柄；不绘制轨道，滚动时出现、闲置 900ms 后以 240ms 渐隐，并支持真实拖动定位。
- 收藏浮层的原生文件夹 `<select>` 替换为 Fluid Functionalism 风格的自绘下拉，支持滚动列表、弹性进入动画、鼠标选择和方向键 / Home / End / Enter / Escape 操作。
- 全局输入框、搜索框与收藏浮层焦点统一为中性 1px 边界，不再出现蓝色大描边；按钮仍保留中性可访问焦点提示。
- 设置齿轮不再跳转智能管理页，改为打开侧边栏内真实设置页；页面展示本地收藏、待同步数量和现有 Google 登录状态，并复用真实 Google OAuth 登录/退出链路。
- 设置页支持 AI API Key 的格式与在线权限验证；Key 只写入当前扩展的 `chrome.storage.local`，UI 只返回配置状态和尾号，不回显完整值。
- AI 云端调用会把用户选择的服务商、模型和 Key 作为自定义请求头临时传给已认证的 Supabase Edge Function；函数不持久化该 Key。
- 增加 Gemini、OpenAI、DeepSeek 官方 API 域名的精确 `host_permissions`，并补充 AI 设置存取/失败不落盘测试。
- 本地预览已验证：顶部/底部遮罩变量、滚动手柄拖动与闲置渐隐、自定义下拉键盘选择、设置页保存状态、收藏浮层/设置输入框焦点和无控制台错误。

### 2026-07-29 · 收藏 UI 与网站图标

- 收藏管理页与侧边栏对齐 `ai-coding-Research` 的 Cosmos “我的收藏”视觉语言：中性页面、柔和收藏卡、胶囊筛选、统一字号与间距。
- 管理页新增可用的“全部 / 已理解 / 待处理”状态筛选，并补齐桌面和窄屏布局。
- 新增统一 `SiteIcon` 组件；书签树、地址栏建议和收藏卡均按网址显示站点 favicon。
- 使用 Chrome Manifest V3 官方 `_favicon` 服务读取本地 favicon 缓存，旧书签无需重新保存即可显示图标；加载失败时使用本地图形兜底。
- 控件中的字符图标替换为统一线性 SVG 图标，补齐 favicon 权限。
- 书签树展开箭头改为统一 SVG，并以 `16px / 24px / 自适应文字` 三列网格固定箭头、文件夹和标题位置；六行文件夹的横向位置及箭头/图标垂直中心已逐项验证一致。
- 增加仅开发环境可用的侧边栏评审画布 `sidepanel.html?preview=1`，使用同一套真实 React 组件与样式，方便浏览器内直接查看和标注；预览数据不会进入生产构建。
- 根据浏览器标注重构侧边栏底部：移除原搜索框和账号服务条，将“智能管理”收纳为右上角 Sparkles 图标入口。
- 底部新增固定的“问收藏 Agent”输入框；提交自然语言后打开智能管理页并执行语义检索，云端不可用时沿用现有本地收藏搜索回退。
- 根据用户提供的 ChatGPT 输入框参考，再次收敛 Agent 输入区：采用单一大圆角容器、上方文本输入、下方操作栏和右侧圆形发送键，只借用布局框架，不复制权限、模型、麦克风等功能。
- 输入区再次按标注精简：去掉“收藏 Agent”和“Enter 发送”，发送键缩为 32px 视觉尺寸并保留 44px 点击热区；输入框聚焦改为中性容器状态，不再出现蓝色方框。
- 书签同层拖拽改为实时乐观排序，并用 240ms FLIP 位移动画平滑挤开相邻条目；松手后写入 Chrome，`prefers-reduced-motion` 下关闭位移动画。开发预览已补齐真实排序状态，不再用错误提示拦截。
- 去掉书签树的“书签栏 / Google / 数量”根级标题，主书签栏进入后直接展示内部文件夹；其他 Chrome 根目录仍作为顶层文件夹保留，避免数据入口丢失。
- 右上角智能管理图标替换为统一 Lucide 设置齿轮；本轮进一步改为打开侧边栏设置页。
- 参考 Fluid Functionalism 的 `scroll-fade` 实现，为书签滚动区增加 52px 底部 `mask-image` 渐隐，并用滚动时间线在到达列表底部时自动撤销遮罩。
- 文件夹展开收起改为可逆的 320ms Grid 高度、透明度与轻位移动画；收藏浮层采用 180ms 遮罩淡入和 320ms `0.97 → 1` 面板缩放进入，均受减少动态效果设置控制。
- 开发预览补齐收藏所需的文件夹列表、当前页面捕获和保存响应，右上角星标现在能打开并验证真实表单，不再显示预览写入错误；预览浮层被限制在侧栏画布内部。
- 已完成 1440px 管理页、390px 管理页和 380px 侧边栏浏览器视觉检查。

### 2026-07-29 · 0.2.3 可靠性加固

- 初始化 `main` Git 仓库，基线提交为 `cf619df`。
- 离线队列改为保留全部待同步项目；元数据更新不会覆盖已排队正文。
- 同步失败使用最长六小时的指数退避，失败任务不会阻塞后续任务。
- 队列完成和失败写入增加 revision 校验，避免并发变更被旧请求删除。
- 启动同步改为先推送本地队列、再拉取云端，避免旧云端状态覆盖待同步数据。
- 已收藏页面再次点击星标时复用原书签文件夹，不再默认向书签栏创建重复项。
- 删除书签或整个文件夹前必须二次确认。
- 弹窗增加焦点约束、Escape 关闭、可见焦点样式和减少动态效果支持。
- 项目固定 Node.js 22.22.2，并补充 Node 版本前置检查。
- 增加交付物生成与版本校验脚本；历史误标解压目录已移入 `outputs/legacy-mislabeled/`，未删除。

## 已完成内容

- 建立 React、TypeScript、Vite、Manifest V3 工程。
- 实现 Chrome 原生书签栏侧边栏和完整收藏管理页。
- 侧边栏直接读取当前 Chrome 书签树，优先选择账号同步的书签栏。
- 同时展示账号书签栏、本机书签栏、其他书签和移动设备书签，避免 Chrome 星标保存位置不同导致遗漏。
- 右键收藏改为自动打开侧边栏，并预填页面或链接地址、标题和选中文字。
- 右键草稿按标签页隔离，消费后立即清理，避免跨页面串用。
- 侧边栏新增 Google 账号与 AI 同步状态入口。
- 支持原生书签与文件夹的新增、改名、URL 修改、删除和拖放移动。
- 监听原生书签新增、改名、移动、重新排序和删除并实时刷新。
- 实现统一输入框：书签、历史记录、已打开标签页联想、网址直达和 Chrome 默认搜索引擎。
- 实现当前网页正文、Canonical URL、描述、作者、站点、图片和选中文字提取。
- 正文提取排除表单、输入框、脚本、导航等高风险内容。
- 保存时创建或更新真实 Chrome 原生书签。
- 支持选择 Chrome 原生文件夹。
- 建立规范化 URL 与 SHA-256 资源键，不依赖跨设备不稳定的 Chrome bookmark ID。
- 使用 IndexedDB 建立可扩展的本地缓存和离线补同步队列。
- 实现现有 Chrome 书签自动索引，不再提供手动导入步骤。
- 实现后台自动补同步和五分钟周期重试。
- 实现云端缺失书签恢复到 Chrome。
- 实现 Supabase Google OAuth PKCE 登录代码。
- 强制产品 Google 账号与当前 Chrome 配置文件账号一致。
- 建立 Supabase 数据表、RLS、复合主键和 pgvector 索引。
- 实现认证 Edge Function：
  - Gemini 结构化摘要、标签、主题、关键点和适用场景。
  - Gemini 768 维 Embedding。
  - pgvector 语义搜索。
- AI 提示对网页内 Prompt Injection 做显式隔离。
- 模型密钥默认从服务端 Secret 读取；用户配置 BYOK 后，调用会优先使用扩展本地保存并临时转发的 Gemini Key。
- 构建时只申请实际 Supabase 项目域名，不使用 `<all_urls>` 或 Supabase 通配域名。
- 增加项目说明、架构文档和云端配置说明。
- 已保留可直接用于 Chrome“加载已解压的扩展程序”的 0.2.2 历史交付物；0.2.3 尚未生成正式交付包。
- 已生成扩展 ZIP 和完整源码 ZIP 交付包。

## 下一步计划

1. 获得用户授权后创建正式 Supabase 项目并应用数据库迁移。
2. 创建 Google Auth Platform Web OAuth Client，启用 Supabase Google Provider。
3. 将实际扩展回调 URL 加入 Supabase Redirect URLs。
4. 配置 Gemini 服务端密钥并部署两个 Edge Function；部署前需再次确认新增的 OpenAI / DeepSeek BYOK 请求头与服务商路由。
5. 获得确认后生成 0.2.3 交付包，并在 Chrome 扩展管理页重新加载，验证右键拉起编辑表单、Chrome 星标、删除确认和完整书签根目录。
6. 进行真实端到端验证：
   - Google 账号匹配和错账号拦截。
   - 书签树实时读取、网址打开、默认搜索、历史与标签页联想。
   - 新建、改名、拖放移动、删除和保存后原生书签落盘。
   - Chrome Sync 跨设备出现。
   - 摘要、标签和语义搜索结果。
   - 离线保存、恢复网络后补同步。
   - 云端恢复缺失原生书签。
7. 设计删除传播、回收站和冲突解决策略。
8. 根据真实体验决定是否替换 `chrome://bookmarks`。

## 遇到的问题

- Chrome 原生书签只能保存标题、URL 和文件夹，不能承载摘要和标签。
- Chrome 扩展同步空间不足以存放正文和 AI 索引。
- Google OAuth 和生产扩展 ID 必须由真实 Google Cloud、Supabase 和 Chrome Web Store 配置共同完成。
- Chrome 扩展不能复制地址栏未公开的计算器、站点搜索快捷词等内部逻辑。

## 已解决问题

- 使用 Chrome Sync 与 Supabase 双层同步模型。
- 使用 Google 账号和 Chrome 配置文件邮箱一致性检查避免账号串库。
- 使用 Supabase RLS 隔离每个用户的数据。
- 使用本地队列保证云端失败时原生书签仍能成功保存。
- 使用规范化 URL 资源键解决跨设备 Chrome bookmark ID 不可靠问题。
- Chrome 作为基础字段的唯一事实来源；智能索引自动关联，不再要求用户导入。
- 地址栏联想查询只在本机、只在用户输入时发生，不把完整历史记录上传云端。

## 未解决问题

- 尚无 Supabase、Google OAuth 和 Gemini 的真实项目凭据。
- 已完成 0.1.0 真实 Chrome 解压安装；0.2.3 构建已通过，但打包、重载和交互验收待进行。
- 删除原生书签目前不会立即永久删除云端资源，需要回收站产品规则。
- 同一个 Canonical URL 的多位置、多备注模型目前合并为一个资源。
- Chrome 移动端没有扩展运行环境，后续需要 Web/PWA 管理端。
- 正式品牌名、图标和商店素材尚未确定。
- 云端 AI 调用尚未建立正式的按用户限流和费用配额；本轮多服务商 Edge Function 源码尚未发布。

## 验证情况

- `npm run typecheck`：通过。
- `npm run test`：13 个测试文件、49 个测试通过。
- `npm run build`：通过，已生成 `dist/`。
- Playwright 浏览器视觉检查：管理页桌面/移动端、侧边栏书签树/搜索建议均通过；无控制台错误。
- 侧边栏对齐评审：六行箭头 X 坐标均为 321px、文件夹图标 X 坐标均为 345px、标题 X 坐标均为 377px；每行箭头与文件夹图标垂直中心完全一致。
- Agent 输入框提交、清空和智能管理跳转请求已在本地评审页验证；账号服务区已确认不再渲染，控制台无错误。
- 本地评审页已实际把“工作与内部系统示例收藏”从第 1 位拖到第 5 位，顺序成功持久化且无控制台错误；输入框聚焦时实测 `outline: none`，仅保留中性容器边界。
- 渐隐遮罩已验证：顶部时 `--bookmark-fade-start=1 / --bookmark-fade-end=0`，离开顶部后 start 变为 `0`，滚动到底 end 自动变为 `1`；自定义手柄实测可从 `scrollTop=220` 拖到 `453`，闲置后 `opacity=0`。展开面板使用 320ms `cubic-bezier(0.16,1,0.3,1)`，收藏浮层进入过程中实测同时存在透明度、位移和缩放。
- 设置页与自定义下拉已在本地预览验证：Key 保存后只显示 `•••• + 后四位`；下拉可用 ArrowDown + Enter 完成选择；收藏浮层和设置输入框聚焦均为 `outline: none` 与单层中性 1px 边框。
- AI 服务商预设已验证：OpenAI 切换后模型为 `gpt-5.6-luna`，DeepSeek 切换后模型为 `deepseek-v4-flash`，未填写对应 Key 时保存按钮保持禁用。
- `npm run verify:artifacts`：通过；当前活动交付目录和历史 ZIP 版本一致。
- 两个 Supabase Edge Function 已完成独立 TypeScript 打包解析检查。
- 生产构建已检查，不包含源码映射、测试数据或项目密钥。
- npm 依赖审计：0 个已知漏洞。
- 书签知识卡实测为白底、无投影、约 5.5% 中性色描边和 14px 圆角；扫描前后分别为紧凑/展开状态，内容自然换行。编辑页可查看完整 AI 简介与主题，新增“用户精选”标签保存后重新打开仍存在，并显示“采用你的版本”；浏览器控制台无错误。
- 尚未声称 Google OAuth、Supabase、Gemini、Chrome Sync 已完成真人端到端验证。

## 暂不应并行修改

在真实云端联调完成前，其他 Agent 不应同时修改以下核心协议文件：

- `src/lib/auth.ts`
- `src/lib/cloud.ts`
- `src/extension/background.ts`
- `supabase/migrations/202607290001_initial.sql`
- `supabase/functions/`
